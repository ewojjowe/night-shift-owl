# --- Web framework ---------------------------------------------------------
fastapi==0.115.6            # ASGI web framework that gives us routing + OpenAPI docs for free
uvicorn[standard]==0.34.0   # ASGI server that actually runs the app (with --reload in dev)

# --- Data validation -------------------------------------------------------
pydantic==2.10.4            # Request/response models + runtime validation
pydantic-settings==2.7.1    # Loads configuration from environment variables into a typed object
email-validator==2.2.0      # Enables Pydantic's EmailStr type

# --- Database --------------------------------------------------------------
motor==3.6.0                # Async MongoDB driver (asyncio-friendly wrapper over pymongo)

# --- Auth / security -------------------------------------------------------
passlib[bcrypt]==1.7.4      # Password hashing (bcrypt) so we never store plaintext passwords
bcrypt==4.0.1               # Pinned <4.1 so passlib 1.7.4 doesn't emit its version-detection warning
python-jose[cryptography]==3.3.0  # Encode/decode JWT access tokens
python-multipart==0.0.20    # Lets FastAPI parse OAuth2 form-encoded login requests
